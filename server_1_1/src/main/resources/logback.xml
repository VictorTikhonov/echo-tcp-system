<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="SERVER_RESPONSE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level %logger
                message = %msg
                remote = %X{remote}
                thread = %X{thread}%n%n</pattern>
        </encoder>
    </appender>

    <appender name="DEFAULT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger -- %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Task 1: TCP blocking -->
    <logger name="ru.tikhonov.task_1.TcpServer" level="INFO"/>

    <!-- Task 3: gRPC -->
    <logger name="ru.tikhonov.task_3.GrpcServer" level="INFO"/>
    <logger name="ru.tikhonov.task_3.EchoServiceImpl" level="INFO"/>

    <!-- Per-response TCP и Netty -->
    <logger name="Benchmark.Server.Response" level="INFO" additivity="false">
        <appender-ref ref="SERVER_RESPONSE"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="DEFAULT"/>
    </root>

</configuration>