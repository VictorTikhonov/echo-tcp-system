<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ======================= APPENDERS ======================= -->

    <appender name="SUMMARY" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger
                message = %msg
                max = %X{maxRTT_ms} ms
                min = %X{minRTT_ms} ms
                total = %X{totalRTT_ms} ms
                avg = %X{averageRTT_ms} ms
                p95 = %X{p95_ms} ms%n
            </pattern>
        </encoder>
    </appender>

    <appender name="REQUEST" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger
                message = %msg
                start = %X{startTime_ms} ms
                end = %X{endTime_ms} ms
                rtt = %X{rtt_ms} ms
                server_time = %X{serverTime_s} s%n%n
            </pattern>
        </encoder>
    </appender>

    <appender name="PARALLEL_SUMMARY" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger
                message = %msg
                clients = %X{clients}
                messages_per_client = %X{messagesPerClient}
                total_messages = %X{totalMessages}
                wall_time = %X{wallTime_ms} ms
                total = %X{totalRTT_ms} ms
                avg = %X{averageRTT_ms} ms
                min = %X{minRTT_ms} ms
                max = %X{maxRTT_ms} ms
                p95 = %X{p95RTT_ms} ms%n%n
            </pattern>
        </encoder>
    </appender>

    <appender name="PARALLEL_REQUEST" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger
                message = %msg
                client = %X{clientId}
                start = %X{startTime_ms} ms
                end = %X{endTime_ms} ms
                rtt = %X{rtt_ms} ms
                server_time = %X{serverTime_s} s%n%n
            </pattern>
        </encoder>
    </appender>

    <appender name="GRPC_SUMMARY" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger
                message = %msg
                sent = %X{sent}
                received = %X{received}
                total_time = %X{totalTime_ms} ms
                avg_rtt = %X{avgRtt_ms} ms
                throughput = %X{throughput_msgPerSec} msg/s
                sequence_ok = %X{sequenceOk}
                content_ok = %X{contentOk}
                server_time_ok = %X{serverTimeOk}%n%n
            </pattern>
        </encoder>
    </appender>

    <appender name="NETTY_SUMMARY" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger
                message = %msg
                clients = %X{clients}
                messages_per_client = %X{messagesPerClient}
                total_messages = %X{totalMessages}
                wall_time = %X{wallTime_ms} ms
                throughput = %X{throughput_msgPerSec} msg/s
                total = %X{totalRTT_ms} ms
                avg = %X{averageRTT_ms} ms
                min = %X{minRTT_ms} ms
                max = %X{maxRTT_ms} ms
                p95 = %X{p95RTT_ms} ms%n%n
            </pattern>
        </encoder>
    </appender>

    <appender name="NETTY_REQUEST" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger
                message = %msg
                client = %X{clientId}
                start = %X{startTime_ms} ms
                end = %X{endTime_ms} ms
                rtt = %X{rtt_ms} ms
                server_time = %X{serverTime_s} s%n%n
            </pattern>
        </encoder>
    </appender>

    <appender name="DEFAULT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger -- %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ======================= LOGGERS ======================= -->

    <!-- Сводки: INFO (1 запись в конце прогона) -->
    <logger name="Benchmark.Summary" level="INFO" additivity="false">
        <appender-ref ref="SUMMARY"/>
    </logger>

    <logger name="Benchmark.Parallel.Summary" level="INFO" additivity="false">
        <appender-ref ref="PARALLEL_SUMMARY"/>
    </logger>

    <logger name="Benchmark.Grpc.Summary" level="INFO" additivity="false">
        <appender-ref ref="GRPC_SUMMARY"/>
    </logger>

    <logger name="Benchmark.Netty.Summary" level="INFO" additivity="false">
        <appender-ref ref="NETTY_SUMMARY"/>
    </logger>

    <logger name="Benchmark.Request" level="WARN" additivity="false">
        <appender-ref ref="REQUEST"/>
    </logger>

    <logger name="Benchmark.Parallel.Request" level="WARN" additivity="false">
        <appender-ref ref="PARALLEL_REQUEST"/>
    </logger>

    <logger name="Benchmark.Netty.Request" level="WARN" additivity="false">
        <appender-ref ref="NETTY_REQUEST"/>
    </logger>

    <logger name="ru.tikhonov.benchmark.BlockingBenchmark" level="DEBUG" additivity="false">
        <appender-ref ref="DEFAULT"/>
    </logger>

    <!-- ======================= ROOT ======================= -->

    <root level="INFO">
        <appender-ref ref="DEFAULT"/>
    </root>

</configuration>